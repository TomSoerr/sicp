// The reverse order is the result of building the answer list form the first to the last element. The current answer is defined as pair(current, last) so the first result will be the last in the list

// The second try results in a answer of the for: list(list(list(c), b), a)
